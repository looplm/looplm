{
  "name": "looplm",
  "version": "0.19.0",
  "private": true,
  "description": "LLM debugging platform — from traces to fixes",
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:web": "pnpm --filter @looplm/web dev",
    "dev:api": "cd apps/api && poetry run uvicorn app.main:app --reload --port 8000",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "dependencies": {
    "langfuse": "^3.38.6"
  }
}
